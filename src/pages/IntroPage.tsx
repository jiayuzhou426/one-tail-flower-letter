import {Fish} from '../components/Fish';import {Ripple} from '../components/Ripple';import {useGame} from '../GameContext';
export function IntroPage({start}:{start:()=>void}){const {data,patch,reset}=useGame();const doReset=()=>{if(confirm('重置花塘、图鉴和花瓶吗？'))reset()};return <main className="intro page"><button className="sound" onClick={()=>patch({soundEnabled:!data.soundEnabled})}>{data.soundEnabled?'◖':'◌'} 声音</button><div className="intro-water"><Ripple/><Fish/></div><section><p className="eyebrow">一封来自水路的信</p><h1>一尾花信</h1><p className="quote">“水偶尔会把自己游成一条鱼。”</p><button className="primary" onClick={start}>随水远游 <span>→</span></button></section><button className="quiet reset" onClick={doReset}>重置花塘</button></main>}
