import {useState} from 'react';import {GameProvider,useGame} from './GameContext';import type {Page} from './types';import {flowers} from './data/flowers';import {IntroPage} from './pages/IntroPage';import {ExplorePage} from './pages/ExplorePage';import {PondPage} from './pages/PondPage';import {ArrangePage} from './pages/ArrangePage';import {BottomNav} from './components/BottomNav';import {Fish} from './components/Fish';import {Ripple} from './components/Ripple';
function Shell(){const {data,receive}=useGame();const [page,setPage]=useState<Page>(data.hasSeenIntro?'pond':'intro');const [message,setMessage]=useState(false);const start=()=>setPage('explore');const finish=(_biome:string,rainy:boolean)=>{setPage('transition');setMessage(true);setTimeout(()=>{const undiscovered=flowers.filter(f=>!data.discoveredFlowerIds.includes(f.id));let chosen='lotus';if(data.totalTrips>0){const candidates=(Math.random()<.7&&undiscovered.length?undiscovered:flowers);const rainPick=rainy&&Math.random()<.45?flowers.find(f=>f.id==='nameless'):undefined;chosen=(rainPick||candidates[Math.floor(Math.random()*candidates.length)]).id}receive(chosen);setPage('pond')},2050)};return <div className="app-shell">{page==='intro'&&<IntroPage start={start}/>} {page==='explore'&&<ExplorePage finish={finish}/>} {page==='transition'&&<main className="transition page"><Ripple/><Fish/><p className={message?'show':''}>十秒到了。<br/>鱼把自己还给了水。</p><i className="drop"/></main>} {page==='pond'&&<PondPage openArrange={()=>setPage('arrange')}/>} {page==='arrange'&&<ArrangePage/>}{data.hasSeenIntro&&page!=='transition'&&<BottomNav page={page} setPage={setPage}/>}</div>}
export default function App(){return <GameProvider><Shell/></GameProvider>}
